// Stub: SQLite knowledge embedder

import type { KnowledgeItem } from './types';

export async function embedText(text: string): Promise<number[]> { return []; }
export async function embedAndStore(items: KnowledgeItem[]): Promise<void> { }
